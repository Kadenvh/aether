\* Apalache config for the AETHER ledger model (U14). INIT/NEXT form (Apalache
\* prefers this over SPECIFICATION for non-canonical stuttering specs).
INIT Init
NEXT Next

\* Bound the chain length so the check terminates.
CONSTANT MaxLen = 5

\* The structural ledger invariant proven over all reachable states.
INVARIANT Inv
